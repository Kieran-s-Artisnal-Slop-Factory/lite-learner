---
title: Triggers
lessons:
  - audit-log-trigger
  - revision-trigger
  - validation-trigger
  - cascade-trigger
---

A trigger is SQL that runs automatically in response to an `INSERT`, `UPDATE`,
or `DELETE` — the database enforcing logic on itself. Use them for audit logs,
derived values, validation, and cascading changes.
