---
title: Filtering Data
lessons:
  - filter-with-where
  - ranges-with-between
  - search-with-like
  - handling-nulls
  - combining-filters
---

Most of the time you want *some* rows, not all of them. `WHERE` filters the
results — by equality, numeric ranges with `BETWEEN`, text patterns with
`LIKE`, `NULL` checks, and combinations of all of these.
