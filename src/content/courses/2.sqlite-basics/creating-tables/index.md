---
title: Creating Tables
lessons:
  - create-table
  - column-types
  - default-values
  - create-table-if-not-exists
---

Time to build your own. `CREATE TABLE` defines a table's columns and types,
marks its primary key, sets default values, and — with `IF NOT EXISTS` — stays
safe to run more than once.
