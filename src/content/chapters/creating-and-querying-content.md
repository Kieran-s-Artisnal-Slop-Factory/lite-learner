---
title: Creating and Querying Content
exercises:
  - insert-into
  - select-rows
---

A table without rows isn't much of a database. In this chapter you'll add
rows with `INSERT INTO`, then read them back with `SELECT` — the statement
you'll use more than any other.
