---
title: Common Table Expressions (CTEs)
exercises:
  - simple-cte
  - multiple-ctes
  - recursive-numbers
  - recursive-tree
---

A CTE (`WITH ... AS`) names a subquery so you can build a big query out of
readable, named steps — and, when recursive, walk hierarchies and generate
sequences that plain `SELECT`s can't.
