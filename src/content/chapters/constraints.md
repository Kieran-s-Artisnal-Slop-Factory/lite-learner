---
title: Constraints
exercises:
  - not-null-constraint
  - unique-constraint
  - check-constraint
  - foreign-key-constraint
  - default-value
---

Constraints are rules the database enforces for you, rejecting bad data before
it ever lands. This chapter covers `NOT NULL`, `UNIQUE`, `CHECK`,
`FOREIGN KEY`, and `DEFAULT`.
