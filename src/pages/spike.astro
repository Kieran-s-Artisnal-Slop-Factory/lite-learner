---
// Phase 0 throwaway spike page — de-risks the exercise engine before content
// is authored around it. Delete once the real exercise page (Phase 2) lands.
import Layout from '../layouts/Layout.astro';
import SpikeApp from '../components/spike/SpikeApp.svelte';
---

<Layout title="Spike">
  <SpikeApp client:only="svelte" />
</Layout>
